# DEPRECATED

This Firebase Cloud Functions backend has been migrated to Vercel Serverless Functions.

The new backend code is located in the `/api` directory at the project root.

## Migration Details
- **Old**: Firebase Cloud Functions (`/functions`)
- **New**: Vercel Serverless Functions (`/api`)

## Deployment
Use `npm run deploy:backend` to deploy to Vercel.

This directory is kept for reference only and will be removed in a future release.
